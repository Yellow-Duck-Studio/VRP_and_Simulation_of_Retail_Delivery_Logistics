# Backend API URL for local development
# When running with `npm run dev`, set this to your backend URL
# Example: VITE_API_BASE_URL=http://localhost:3001
# In production (Docker), leave empty to use relative URLs through nginx proxy
VITE_API_BASE_URL=http://localhost:3001
